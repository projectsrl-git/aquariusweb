Fix bilancio (toggle/tabs/quadratura/align) and /documenti ORDER BY

/documenti (ristampa documenti): DocumentArchiveDao built
"ORDER BY <col> <dir>, ORD_NUMORD <dir>" — sorting by number duplicated the
column and SQL Server rejected it. Tie-breaker now added only when the sort
column isn't already ORD_NUMORD.

Bilancio:
- "Mostra conti clienti/fornitori" toggle: unchecked checkbox sent nothing so
  defaultValue=true always won; now a hidden cf field updated by JS.
- Tabs for Stato Patrimoniale / Conto Economico (no more scrolling to switch).
- Section totals moved to a card-footer so the two columns' totals align at the
  bottom (h-100 + align-items-stretch).
- Quadratura banner: checks Attivo - Passivo == result (tol 0.01), shows the
  imbalance otherwise.

Version 0.5.1. Sandbox checks only; confirm build/SQL on deploy.
