Legami viewer: remove inline SVG card (node detail still empty)

The previous &amp; fix did not resolve the empty node detail. The inline SVG card
(th:utext) was the regression vs the working Fable5 node view, so it is removed -
both the template card and the controller's graphSvg attribute / neighborhoodSvg
call. The node detail returns to the proven structure (header + "used by" +
"opens/calls/prints/migrates") with all grid improvements intact (counter filters,
pagination, inline search, menu-orphan fix, form titles).

The neighborhood graph will be re-added in isolation (an endpoint serving the SVG as
a separate image/svg+xml resource, embedded via <object>) so a problematic SVG can
never break the node page.

Version 0.23.2. Sandbox syntactic checks only (no Maven build).
