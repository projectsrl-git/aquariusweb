Add sales document controls: unpaired DDT, number sequences

Patch B of session 10 - apply AFTER patch A (shares the clienti menu
case and version log). /vendite/controlli: three read-only checks with
DECLARED criteria, anomaly-panel pattern (anomalies surfaced, never
corrected). Version bump 0.19.0 -> 0.20.0.

1. DDT non/parzialmente fatturati - grounded on the session-1 verified
   mechanism: invoicing STAMPS the DDT ROWS with MOV_NUMFAT/MOV_DATFAT
   on U_BOL_DD. LEFT JOIN U_BOL_TT->DD, HAVING stamped < total rows;
   stato NON_FATTURATO / PARZIALE; causale column shown because some
   causali legitimately never invoice (resi, omaggi, c/lavoro) -
   exclusion list is a NEEDS_DOMAIN question in the tracker.
2. Invoice number sequence - gaps via LEAD() over distinct
   TRY_CONVERT(INT, ORD_NUMORD) per year; duplicates listed with the
   TIPORD series involved (different series may be legitimate);
   non-numeric numbers surfaced as a data-quality note, not silently
   dropped. All series together: per-TIPORD refinement pending domain
   confirmation.
3. IVA sales register protocol gaps - LEAD() over IVA_CHIAVE (U_IVA_CL,
   non-corrispettivi rows) per year. The legacy PRNPROT checks MOV_CONT
   instead (MOV_NDOC = protocollo, verbatim comments) - tracked as
   NEEDS_DOMAIN on which sequence is fiscally authoritative.

CONTROLLO_BOLLEFATTURE turned out to target FTP/punto-vendita archives
(u_ftp_an, casse): different domain, precise NEEDS_DOMAIN question
filed for Erasmo. Menu: "Controlli vendite" added to the clienti case.

Verification (sandbox only): as patch A; chain verified on fresh clone
of HEAD bb97ba5. LEAD() window and FOR XML PATH aggregation to be
confirmed on SQL Server 2012 at first deploy.
