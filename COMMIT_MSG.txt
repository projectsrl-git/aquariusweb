Complete customer registry tabs (18 of 19 active)

Turn the 12 placeholder tabs of the customer master form into real editable
forms, matching the VFP MENU_CLI000 layout.

Fields were recovered from the VFP form (MENU_CLI000.SCT) and then
cross-checked against the real schema (U_CLI_AN, 254 columns): only columns
that actually exist were mapped. 123 fields added across 13 areas (Vettori,
Contabili, Posticipi riba, Fido, Rif. cliente, Web, Legali, Produzione,
Gruppi E-mail, Testi, Fatturare a, Intrastat, Fattura elettronica), typed
from the schema (varchar/numeric/bit/text -> String/BigDecimal/Boolean/text).

Distinte RID stays a placeholder: none of the CLI_*RID columns from the VFP
form exist in the current U_CLI_AN schema, so there is nothing to bind. It is
documented rather than faked (plug&play rule: never invent legacy columns).

- Customer.java: +123 @Column fields.
- CustomerController.java: 12 tabs marked active; copyEditableFields extended
  with the 123 setters (whitelist save, @DynamicUpdate).
- clienti/edit.html: 13 placeholder panes replaced with real fields by type.

Verification: Java brace/paren scan, all 159 th:field map to entity
properties, edit.html div balance 232/232 and 19 unique tab ids, git apply
--check clean on a fresh clone of HEAD 7a03157. Live save round-trip to
confirm on deploy.
