Fix invisible active tab and show all detail cards

Two customer-registry UI fixes.

1. Active form tab was invisible (white text, no background). The rule
   .form-shell-nav .nav-link.active used var(--aq-primary), which was never
   defined in :root, so the background stayed transparent. Define
   --aq-primary (brand navy #0c4a6e); this also fixes other uses of the var.

2. The customer detail view showed only two cards. Rewrote detail.html to
   render one card per data area (anagrafica, contatti, IVA, commercio, banca,
   fido, sede legale, referente, web, note) across the full page with natural
   scrolling. Optional cards appear only when they hold data, so no empty
   boxes. Moved the action buttons to the top.

Verification: style brace balance 97/97, detail.html div balance 45/45,
every referenced entity property exists, git apply --check clean on a fresh
clone of HEAD 74bfa73. Visual result to confirm on deploy.
