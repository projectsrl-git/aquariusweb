Add cespiti categories view with per-category summary

Part 2 of 2 - apply AFTER the register patch. /cespiti/categorie:
category list (u_amm_ca) enriched with a per-category aggregate
computed live from u_amm_at via JPQL GROUP BY: asset count, total
historical value, total depreciated, total residual. Plain sums of the
anagrafica columns - no netting, no fiscal math - and the subtitle says
so. Each row links to the register filtered by that category; the
register gains the "Categorie" button.

Verification (sandbox only): checks as in part 1; chain verified on
fresh clone of HEAD 3b54f20.
