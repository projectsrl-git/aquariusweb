Bilancio: add side-by-side (sezioni contrapposte) view switch

New ?vista=mastri|contrapposte switch (btn-group next to the C/F toggle):
- mastri: existing mastro/gruppo tree with tabs.
- contrapposte: Attivita|Passivita side by side, then Costi|Ricavi side by side
  (saldo per account, section total in footer) — the pre-grouping layout.
C/F leaves still respect the toggle; section totals include everything so the
quadratura is unchanged. New sezioneFlat fragment; controller gains a vista param.

Version 0.8.0. Sandbox checks only.
