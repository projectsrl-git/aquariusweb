Bilancio N-1 comparison + clienti/fornitori aging scadenziario

Two accounting consultations delivered together.

1) Bilancio di verifica - previous-year comparison (N-1): "Confronta con anno
   precedente" option adds "Saldo anno prec." and "Variazione" columns at
   mastro/gruppo/account level, computed for the previous year (same month range).

2) Scadenziario clienti/fornitori: read-only open-items (residual = total - paid,
   PART_CLI/PART_FOR) aggregated per party with aging by days overdue
   (a scadere, 0-30, 31-60, 61-90, >90) + totals. New findAperte() queries,
   ScadenziarioController at /contabilita/scadenziario?tipo=clienti|fornitori,
   ScadenzaRiga DTO, template, and a synthetic "Scadenziario" menu voice.

Versions 0.17.0 (N-1) + 0.18.0 (scadenziario). Sandbox checks only.
