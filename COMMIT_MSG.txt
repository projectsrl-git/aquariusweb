Activate CEE menu entry + make bilancio key figures prominent

- Bilancio CEE menu: Fable 5 had wired it via guessed FORM_TO_URL form names that
  don't match the legacy tbl_menu COMANDO, so it never showed. Replaced with a
  synthetic "Bilancio CEE" entry under Contabilita (always present); removed the
  five guessed FORM_TO_URL entries to avoid missing/duplicate voices.
- Synthesis: the Stato Patrimoniale card now also shows Differenza
  (Attivita - Passivita), coloured like the result.
- Quadratura: replaced the thin banner with a prominent card (same size as the
  synthesis cards): "(Attivita - Passivita) = result, Differenza = 0", green when
  balanced / red when not.

Version 0.8.1. Sandbox checks only.
