Bilancio: Excel export + prominence tweaks

- Excel export: "Esporta Excel" button -> GET /contabilita/bilancio/export
  (ResponseEntity<byte[]>, Apache POI, same pattern as the warehouse valuation).
  New BilancioExcelExporter: "Dettaglio" sheet (one row per account with numeric
  Dare/Avere/Saldo, filterable) + "Sintesi" sheet (totals + quadratura).
- "Il bilancio quadra" text enlarged; the quadratura card's "Differenza" label
  renamed to "Quadratura"; the Stato Patrimoniale / Conto Economico summary cards
  now have a visible border.

Version 0.9.0. Sandbox checks only; confirm build/SQL on deploy.
