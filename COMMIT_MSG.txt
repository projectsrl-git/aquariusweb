Add read-only articles registry (/articoli)

New consultation module for U_ART_PR (254-column legacy article master),
web counterpart of VFP MENU_ART000 ("Anagrafica articoli", 986 objects).

- Article entity: ~30 columns mapped, all insertable/updatable=false,
  verified against docs/db_schema/aquarius_schema_full.csv. Note: the VFP
  form binds M.art_coord1..4 which are memvars; the real column is ART_COORD.
- Shared list pattern (ListParams + list-tools): free-text search on
  code/description/barcode/supplier, sortable whitelist, badges for
  UM/IVA/commodity category/Obsoleto.
- Detail: 4 cards (Anagrafica, Prezzi, Classificazione e fornitore,
  Magazzino e note). No edit path: maintenance stays on the VFP client.
- MenuService FORM_TO_URL: menu_art000 -> /articoli (tbl_menu leaf
  'gestionearticoli' already exists; no synthetic entries needed).
- Migration tracker: 22 rows for MENU_ART000.SCX.

Verification (sandbox only, no Maven/JDK/SQL Server): Java brace/paren
balance OK; duplicate @Column grep empty; fragment arity OK (sortableHeader
8 args, pager 7); tracker CSV 12 columns per row. git apply --check OK on
fresh clone of HEAD d4652ec. Maven build and live-DB behaviour to be
confirmed at first deploy.
