Add read-only sales invoices and proforma modules

New consultation modules for U_FAT_TT/DD (/fatture, VFP MENU_FAT000
"Fatture di vendita") and U_FAP_TT/DD (/proforma, VFP menu_FAP000
"Fatture proforma"). Depends on the DDT patch (applies after it).

Domain corrections vs. the session handoff:
- U_FAP_* is fatture PROFORMA, not fatture acquisto (tbl_menu entry
  'gestionefattureproforma' + form caption confirm it).
- On invoice tables ORD_NUMORD/ORD_DATORD are the invoice's OWN
  number/date (legacy: ord_numord = ALLTRIM(Z_NUMFAT)).

- Invoice/Proforma Head+Row read-only entities (structurally identical
  tables; proforma module is a twin); year-scoped lists; join
  TAGGANCIO = DAGGANCIO.
- FE surfaced read-only: ORD_TRASME badge Trasmessa/Non trasmessa,
  ORD_IDSDI in detail. FE generation stays with Erasmo's
  FATTURAZIONE_ELETTRONICA_LIB (tracked NEEDS_DOMAIN); posting stays
  WRITE_SIDE for Opus sessions.
- MenuService FORM_TO_URL: menu_fat000 -> /fatture, menu_fap000 -> /proforma.
- Migration tracker: 10 rows (MENU_FAT000.SCX + menu_FAP000.SCX).

Verification (sandbox only): brace balance OK, duplicate @Column empty,
fragment arity OK, CSV OK. Full 4-patch chain verified with git apply
--check + apply on fresh clone of HEAD d4652ec. Maven build and live-DB
behaviour to be confirmed at first deploy.
