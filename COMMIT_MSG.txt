Add coverage matrix viewer (/utilita/copertura)

Part 2 (optional per mandate) - apply AFTER the audit patch. Read-only
matrix of the migration coverage audit, MigrationTracker-viewer
pattern: CoverageAuditService (classpath CSV, reuses the RFC-4180
parser of CompanyParameterCatalogService), CoverageController with
per-area matrix + filterable entry list (area, status, free text),
template with clickable matrix and status badges, sidebar entry
"Copertura migrazione" in the Strumenti section of layout.html.

Verification (sandbox only): brace/paren with literal stripping; div
balance; chain verified on fresh clone of HEAD ebf1c44.
