# ============================================================================
#  Profile DEV -- non-sensitive overrides (VERSIONED, no secrets here)
#  Secrets for DEV: config/application-dev.properties (gitignored, external)
# ============================================================================
spring.thymeleaf.cache=false
logging.level.com.aquarius=DEBUG
logging.level.org.springframework.security=DEBUG

# Log directory dedicata per l'ambiente DEV (override: AQUARIUS_LOG_DIR o -Daquarius.log.dir)
aquarius.log.dir=D:/logs/aquariusweb/dev
