# ============================================================================
#  Profile PROD -- non-sensitive overrides (VERSIONED, no secrets here)
#  Secrets for PROD: config/application-prod.properties (gitignored, external)
# ============================================================================
spring.thymeleaf.cache=true
logging.level.com.aquarius=WARN
logging.level.org.springframework.security=WARN

# Log directory dedicata per l'ambiente PROD (override: AQUARIUS_LOG_DIR o -Daquarius.log.dir)
aquarius.log.dir=D:/logs/aquariusweb/prod
